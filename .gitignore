# Build artifacts — generated, not source. The canonical plugin source is plugins/nadlan-config/.
nadlan-config.zip
plugin-build/

# auto-update dist (committed on purpose)
!plugin-dist/
plugin-dist/*
!plugin-dist/nadlan-config.json
!plugin-dist/*.zip

# Local SEO/visual audit cache
.seo-cache/

# Installed JS deps — package.json/package-lock.json are the source of truth
node_modules/

# Private unit-journey acceptance output stays outside the public repository.
# It may contain the protected URL and unlocked screenshots (never the password).
reports/private-unit-journey-release/
reports/local-v2-*.png
docs/qa/private-unit-journey/
scripts/__pycache__/

# Credentials & local secrets — NEVER commit
.env.local
.env
wp-api.ps1

# competitor captures and Google imagery stay private (EcoCity takedown 30.8, competitors own their renders, Google Maps
# Platform terms forbid storing imagery); keep them in C:/Users/777/nad-lan/_private-research/, never in this public repo
docs/research/**/ecocity-cfg/
docs/research/**/*ecocity*
docs/research/**/hauzd/
docs/research/**/worlds/earth-*
docs/research/**/worlds/pilot-google*

# local screenshots and logs of the page journeys (they hold map imagery): never in this public repo
scripts/project-stage/shots/
scripts/project-stage/*.log
**/__pycache__/

# headless Chrome profiles made by assets/skin-a/brand/render-brand.py (empty, checked 25.9.2026)
assets/skin-a/brand/out/ud-*/
