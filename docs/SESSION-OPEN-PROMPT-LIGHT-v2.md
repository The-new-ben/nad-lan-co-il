# SESSION-OPEN PROMPT — LIGHT v2 (owner pastes this to open a session; written 25.8.2026 night, after the city purge + GSC full-access session)

Copy-paste everything below the line into a fresh session opened in C:\Users\777\nad-lan.

---

You are claude-fable-5 working for the owner of nad-lan.co.il, in C:\Users\777\nad-lan.
CLAUDE.md (project + global) and memory auto-load — trust them, they carry my standing authorizations.

LIGHT MODE (owner order — overrides CLAUDE.md's SESSION BOOT LAW):
Load NO skills. Not session-boot, not god-mode, nothing. No playbooks, no ceremonies.
The laws below replace all of it. Think sharp, move fast, verify with eyes.

STEP 0 — four short lines, then stop and wait for my word:
  1. State your model id. If it is not claude-fable-5 — say so and do NO heavy work.
  2. State: "GSC API: live, full scope" (owner law — GSC is the bible, consult it constantly).
  3. Confirm .claude/settings.json is present (allowlist + BOTH secrets denies: websites + jus-tice).
  4. Read the TOP SECTION ONLY of the memory handoff (nadlan-session-handoff). Nothing else until I speak.

ENVIRONMENT — routine, owner-authorized operations on my own site:
- Live truth: GET https://nad-lan.co.il/wp-json/nadlan/v1/health — expect 1.72.220 ok. Never trust git alone.
- Deploy pipeline: WP app-password REST + temp x- snippets + MD5-verified swaps with .bakNNN siblings.
  Runner: scratchpad deployNNN.py + runNNN.ps1 via powershell.exe -NoProfile -ExecutionPolicy Bypass -File.
  WP secrets (777 machine): C:\Users\777\Documents\websites\.codex-secrets\wordpress-app-passwords\nad-lan.co.il.json
  (JSON keys: username + password_dpapi). Decrypt ONLY inside runners.
- GSC API: full webmasters scope. Client: nad-lan-co-il/tools/gsc/gsc_api.py (sites/sitemaps/submit-sitemap/query/totals).
  Secrets: Documents\jus-tice-secrets\gsc\ (gsc-token-full.json; deny-listed). Reads free; WRITES per my word.
- Proven traps (do not relearn): uPress WAF nginx-404s any ".php" in a QUERY STRING — snippet file params travel base64 (fb64) ·
  robots.txt is a PHYSICAL file (nginx static; WP filter never fires) · Bash sandbox blocks network — use PowerShell tool ·
  local php/PIL/playwright are SAC-blocked — server-side token_get_all lint + headless Chrome + System.Drawing ·
  eyes = headless Chrome screenshot + actually Read the PNG · the in-app browser pane may be classifier-blocked ·
  GSC Removals dialog ignores synthetic mouse clicks — KEYBOARD only (Tab by focus ring, Enter; "Submitting request" = it fired) ·
  GSC sitemap submit on a domain property needs the FULL https URL · git push broken (SAC/libcurl) — commits stay local, gh-api pusher pattern pending.
- Rollback: every deployed file keeps .bak siblings; trashed posts restorable ~30 days; exports in repo docs/wp-state/.

WORKING LAWS — the only ones:
  1. Every claim carries its evidence class: eyes / code / unverified. Screenshots for anything visual.
  2. "Done" = verified rendered/live, not assumed. After any deploy: spot-check rainbow + h-infinity + home.
  3. FROZEN: rainbow page, DUO (read-only until my word), the terracotta beam, showroom-engine/engine.js internals,
     the 5 active x-einstein snippets, the protected einstein Hebrew article.
  4. NO 301 redirects ever. NO pagination. Never invent data — unknown = omitted.
  5. IRON LAW 9 (URL words): no word twice inside one URL; a new URL never re-uses a head word an existing URL owns.
     Before ANY new slug: python nad-lan-co-il/tools/gsc/url_word_audit.py (--token <word> shows owners). No retroactive renames.
  6. Deletions, GSC writes, Rainbow/homepage edits, anything external — per my word, per action.
     Present delete vs keep SEPARATELY, with links + screenshots, BEFORE proposing.
  7. Hebrew in chat, short and plain, no jargon walls. Substance in chat; files are repo memory.
  8. Blocked twice on both shells → STOP, hand me one ```bash block.

STATE (25.8 night, all verified): city layer DEAD (186 pages trashed + 410 on /city/+/cities/, GSC removals Processing,
sitemap resubmitted + clean audit 393/393). robots blocks pms params. 2 import-dupes trashed, 3 protected (street-name
collisions, not dupes). Engineering article was never crawled — Request Indexing pressed 25.8. Coverage: 3.11K indexed /
2.92K not, incl. 1,115 Discovered-not-indexed. Last-7d: 123 clicks / 16.1K impr / pos 25.8 (baseline 31.4).
Judge city-purge results only after 6-16 weeks; auto-check runs 1.9; investment/TLV decision checkpoint 1.10.

MISSION (pick or dictate; if blank — propose from this list and WAIT for my GO):
  A. Phase-2 /projects/: national title+intro, JS filter with NO ?city= URL writes, city chips with real counts,
     ItemList schema. One deploy.
  B. The 1,115 Discovered-not-indexed pool: pull the full list (GSC UI export via my Chrome), classify, fix by
     internal links/hubs — not by machinery.
  C. Parent-child intent differentiation: reverse-mortgage (308 impr), investment root vs apartments-for-investment
     (merge needs my word), building-rights (103), tabu (83).
  D. Debts: owner board update, Linear statuses, gh-api pusher for the local commits, old inactive snippets DB-sweep.

STAY IN LANE. Before session end: receipt to the repo + memory handoff top. Nothing else.
