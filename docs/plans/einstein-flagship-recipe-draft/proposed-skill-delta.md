# Applied active-skill delta and traceability record

Applied targets:

- maintained agent skill: `skills/nadlan-flagship-project-showroom/SKILL.md` in
  `The-new-ben/agent-skills`, merged at `e739ae451a9818174f9061a785482a066c2d03ac`;
- repository entry: `skills/recipe-flagship-project-page.md` in this release.

The sections below record the design delta that was applied so future reviewers can trace each
Einstein lesson into the reusable factory.

## 1. Repair the entry point

Original problem:

- the skill calls `docs/playbooks/flagship-showroom-recipe.md` canonical, but that file does not exist;
- it also names a missing H Infinity generator.

Applied change:

- retain the reviewed repository playbook in `flagship-showroom-recipe-v2-draft.md`;
- point the repository skill to that existing file;
- point model generation to a real shared generator interface or archetype generator registry, not an absent project-specific script;
- add a preflight that fails if referenced playbook/scripts are missing.

## 2. Replace the “shape” summary

The short skill now memorizes this sequence:

1. Identity/collision contract.
2. Source and contradiction register.
3. Project-type recipe selection.
4. Buyer decision architecture.
5. Governed project package.
6. HD/LOD/poster and mapping production.
7. Shared versioned engine integration.
8. Private WordPress readback and responsive proof.
9. Fleet regression.
10. Canonical release, rollback and maintenance.

## 3. Replace the GLB rule

Current text: `<1 MB, no normals, uint16`.

Applied rule:

- flagship HD follows the recorded fidelity contract; default high-detail floor is 30,000 meaningful rendered triangles when required;
- normals are required/generated when the viewer's lighting path uses them;
- stable semantic component names are required;
- default working network target is HD <=3 MB, LOD <=400 KB and poster <=200 KB, with measured exception evidence;
- LOD is accepted by silhouette/readability, not by tiny byte count alone;
- triangle padding, hidden filler and copied/unlicensed geometry fail;
- flat-color versus textured material support is declared and visually tested.

This resolves the Einstein conflict without treating the 12 MB safety cap as a performance budget.

## 4. Replace the unknown rule

Current idea: unknown remains unknown forever.

Proposed rule:

> Unknown remains unknown in the active record until a newer cited source, effective date and reviewed replacement event changes its state. History is retained.

This preserves honesty while allowing contractor files to improve the package later.

## 5. Expand the honesty law into capability truth

Retain the ban on invented rooms, sqm, status, direction, price, tour and imagery. Add:

- exact model coordinate is not exact real-world location;
- illustrative direction is not calibrated North/view;
- a schematic View is not a live map;
- a step/door/light simulation is not a spatial tour;
- one draggable object is not a plan-aware configurator;
- prepared-no-write annotation is not OLP delivery;
- project-only inquiry is not selected-unit inquiry;
- a demo label does not upgrade data quality.

Every tool declares L0 teaser, L1 local simulation, L2 connected tool or L3 decision-grade.

## 6. Add shared-engine/no-copy rule

Applied red line:

> New flagships provide a governed data/asset package to one shared versioned engine. Do not copy PHP, JS or CSS runtime code into a project folder. A needed project-specific behavior becomes a reviewed shared provider or remains out of scope.

Project-type recipes configure the engine for residential tower, campus/mixed-use or commercial tower. They do not fork it.

## 7. Retain and clarify the beam freeze

Retain the current beam freeze. Add:

- a v3 map adapter may supply calibrated project/unit state to the shared beam contract;
- no uncalibrated model axis is shown as cardinal direction;
- every map/beam change runs H Infinity, Rainbow, Dimri Yama, Ashira, ToHa2 and The Park regression;
- fallback is context-only, never an invented view.

## 8. Add protected-model and visual-invitation law

The skill summary requires:

- protected stage height at 320/390/landscape/desktop;
- no overlay or sticky control over the model;
- model center hit-test;
- View, Interior, Design and Comments as permanent visual invitations below the model;
- facilities selectable within Interior and from governed facility anchors;
- body-level fullscreen and exact model/scroll/focus restoration;
- 44 px targets and hotspot collision fallback.

## 9. Add zero-inventory and write boundaries

- Zero inventory is a supported state.
- Unit selection, price, availability, plan and unit view require verified inventory fields.
- A zero-inventory flagship may still provide sourced project decisions and project-only inquiry.
- Inquiry uses the existing lead rail; no second endpoint.
- Comments/OLP stays prepare-only until authentication, consent, privacy, retention, idempotency, durable acknowledgement and recoverable failure pass.

## 10. Replace the QA summary

Minimum skill gates link to the full playbook and name:

- fixture/offline tests plus authenticated private WordPress readback;
- 320 x 568, 390 x 844, 568 x 320, 768, 1280 x 800 and 1440;
- poster/model-ready/failure, every anchor/tool, Back, map states, inventory states and lead states;
- one visible H1, one main, canonical/private robots, breadcrumbs/schema owner;
- 44 px targets, no inner/horizontal overflow, contrast, keyboard, screen reader, RTL/LTR and reduced motion;
- route-scoped transfer/LCP/INP/CLS/memory and Save-Data LOD;
- HE first and complete HE/EN/FR/RU/AR siblings before reciprocal hreflang;
- fleet regression and rollback proof.

## 11. Applied concise skill wording

The repository entry remains short while the maintained agent skill carries routed reference detail:

> Build flagships through the shared versioned showroom engine and one governed per-project package. Start with canonical identity and claim-level evidence, select the project archetype, design the buyer decision path, then produce reviewed HD/LOD/poster, mapping and experience assets. Illustration is allowed only in the illustration lane; exact model coordinates never imply real-world precision. Zero inventory is valid. View/Interior/Design/Comments declare their real capability level. The model remains unobstructed and directly operable on mobile. Private WordPress readback, full responsive/accessibility/performance proof, fleet regression, rollback and explicit release are mandatory. Never copy a runtime or create a second canonical/lead path.
