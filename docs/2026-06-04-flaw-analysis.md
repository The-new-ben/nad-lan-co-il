# Flaw Analysis - live nadlan-config 1.42.2

Date: 2026-06-04. Live target: `https://nad-lan.co.il`.

Healthcheck proof: `/wp-json/nadlan/v1/healthcheck` returned `{"plugin":"nadlan-config","version":"1.42.2",...}`. Repository note: `origin/main` is already at `nadlan-config 1.42.3`, and several fixes below appear to exist in main but are not visible on the live site while healthcheck still reports `1.42.2`.

Test method: real headless Chrome at `390x900` and `1440x1000`, plus curl healthcheck/source checks. Evidence artifacts are in `C:\tmp\nadlan-flaw-analysis-2026-06-04\`.

| # | Flaw | REAL? | Live proof (quoted HTML/metric) | Root cause (file/selector) | Severity | Fix direction |
|---|---|---|---|---|---|---|
| 1 | Hero filter pills show raw sprite IDs as text. | REAL on live 1.42.2. | `/projects/`: `<button type="button" class="nldir-pill" data-prof="tama38" style="--pc:#9F6F54;--ps:#F8F0EA"> <span>category-project</span>תמ״א 38 <i>938</i> </button>` and `<span>profession-developer</span>`. `/professionals/`: `<button type="button" class="nldir-pill" data-prof="kablan" style="--pc:#334236;--ps:#F1F4EE"> <span>profession-contractor</span>קבלן <i>2,699</i> </button>`. | Live 1.42.2 render lines in `plugins/nadlan-config/inc/directory.php`: line 290 for professional pills and line 738 for project pills print `esc_html( $pm['icon'] )` inside a plain `<span>`. Current main 1.42.3 already uses `<span class="nldir-pill-mark"><svg><use href="#..."></use></svg></span>` on those same lines. | Blocker | Ship the current main/1.42.3 pill renderer or otherwise replace text sprite IDs with SVG `<use>` icons. This is a visible production defect, not a polish note. |
| 2 | Duplicate H1 on single project/professional pages. | REAL on live 1.42.2. | Rainbow: `<h1 class="wp-block-post-title">Rainbow Tel Aviv – ריינבו תל אביב</h1>` plus `<h1 class="nlpf-name">Rainbow Tel Aviv – ריינבו תל אביב</h1>`. Professional: `<h1 class="wp-block-post-title">גבאי מיכאל</h1>` plus `<h1 class="nlpf-name">גבאי מיכאל</h1>`. | Theme/block template `templates/single.html:10` prints `wp:post-title`. Plugin profile templates print `.nlpf-name` in `plugins/nadlan-config/inc/directory.php:377` and `:479`. Current main 1.42.3 adds a visual-hide rule for `body.single-nadlan_* .wp-block-post-title` in `inc/premium-ui.php:558-570`. | Major | Preferred fix is template-aware: remove or demote the theme post-title on these CPT singles, or ship the 1.42.3 overlay and verify only one visible H1 remains. If SEO semantics matter, avoid leaving two semantic H1s hidden by CSS only. |
| 3 | Archive hero H1 is dark text on the dark blueprint hero. | REAL on live 1.42.2. | `/projects/` hero: `<h1>פרויקטים והתחדשות עירונית</h1>`, computed `color: rgb(27, 26, 23)`, hero background transparent over dark blueprint, measured contrast `1.21:1`. `/professionals/` hero: `<h1>מצאו בעל מקצוע מאומת לנדל״ן</h1>`, same computed color and contrast. | `.nldir-hero` is dark in `plugins/nadlan-config/inc/directory-assets.php`, while `.nldir-hero h1` at line 23 does not force a light color on live. Current main 1.42.3 adds `.nldir-hero h1{color:#FAF8F3!important;text-shadow:...}` in `inc/premium-ui.php:550-556`. | Major | Ship the 1.42.3/current-main hero contrast fix, then recheck contrast on `/projects/` and `/professionals/` at 390 and 1440. |
| 4 | Single project/professional desktop screenshot appears blank cream at 1440. | NOT-REAL as a blank-page/content bug. REAL only as a screenshot artifact caused by flaw #5. | Before mitigation on Rainbow: `scrollWidth:11439`, `clientWidth:1440`, `canScrollRight:true`; DOM was present: `elementFromPoint(10,10)` was the header and `elementFromPoint(center)` was the featured image. First text included `Rainbow Tel Aviv – ריינבו תל אביב Written by in ✓ פרופיל מאומת`. After runtime-only CSS `.nlcard-hp,.nlrev-hp{display:none!important}`, `scrollWidth:1440` and the same page rendered normally. | Not a missing-content render path. The capture was thrown off by off-screen honeypot geometry from `.nlcard-hp` / `.nlrev-hp`; see flaw #5. | Minor | Do not chase a separate blank-page bug. Fix the honeypot overflow, then rerun screenshots. |
| 5 | Single pages have `scrollWidth:11439px` from honeypot inputs at `x:-9999`. | REAL on live 1.42.2. It is visible desktop horizontal overflow; mobile hides it with `overflow-x:hidden`. | Rainbow desktop: `documentScrollWidth:11439`, `documentClientWidth:1440`, `bodyOverflowX:"visible"`, `htmlOverflowX:"visible"`. Professional desktop: same. Honeypot proof: `<input type="text" name="company" class="nlrev-hp" tabindex="-1" autocomplete="off" aria-hidden="true" style="">` with rect `x:-9999,w:30`; professional also has `<input type="text" name="company" class="nlcard-hp"...>` with rect `x:-9999,w:217`. | `plugins/nadlan-config/inc/cards-render.php:95` outputs `.nlcard-hp`, styled at `:154` as `position:absolute;left:-9999px`. `plugins/nadlan-config/inc/reviews.php:208` outputs `.nlrev-hp`, styled at `:246` as `position:absolute;left:-9999px;width:1px;height:1px;opacity:0`. | Major | Replace negative-left hiding with a clipped, non-overflowing honeypot style, for example 1px absolute clipped/inset with `overflow:hidden`, `clip-path:inset(50%)`, `pointer-events:none`, and no negative page geometry. |
| 6 | Homepage has no premium hero above the fold; gray/block section reads unfinished. | REAL as a design/ownership flaw. | Home has no `.nldir-hero` or concept hero. Above-fold H1 is theme/page content: `<h1 class="wp-block-post-title">נדלן חכם: קנייה, מכירה והשקעה עם נתונים</h1>`. The visible injected section is plugin markup inside page content: `<section class="nadlan-home-pillars" dir="rtl"><div class="nhp-head"><p class="nhp-eyebrow">מה תמצאו כאן</p><h2>הכלים והמידע שמצמצמים סיכון בעסקת נדל״ן</h2>...`. | Mixed ownership. Theme/page template `templates/page.html:11` prints the page title. Plugin `plugins/nadlan-config/inc/glossary-autolink.php:154-176` injects `.nadlan-home-pillars`/`.nhp-*`. Plugin `inc/homepage.php:152` and `:234` registers `[nadlan_home_sections]` and `[nadlan_hero_search]`, but automatic append is commented out at `:148`. | Major | Treat homepage as a separate THEME/BLOCKS plus plugin-shortcode job. Decide one canonical premium home hero/page composition, then either wire `[nadlan_hero_search]`/premium sections intentionally or rebuild the WP page blocks. Do not patch directory pages to solve homepage. |
| 7 | Theme post-meta ("Written by" / "in" / date/category shell) appears on CPT profiles. | REAL on live 1.42.2. | Rainbow visible text begins with `Rainbow Tel Aviv – ריינבו תל אביב Written by in ✓ פרופיל מאומת`. Professional visible text begins with `גבאי מיכאל Written by in קבלן רשם הקבלנים #33668`. The same pages also render theme H1/source shell: `<h1 class="wp-block-post-title">Rainbow Tel Aviv – ריינבו תל אביב</h1>`. | `templates/single.html:12` includes pattern `nadlan-revenue/hidden-written-by`. `patterns/hidden-written-by.php:16` prints `<p>Written by </p>`, line 18 prints `wp:post-author-name`, line 20 prints `<p>in</p>`, and line 22 prints `wp:post-terms`. Current main 1.42.3 hides several child post-meta blocks in `inc/premium-ui.php:585-594`, but the raw `<p>Written by</p>` / `<p>in</p>` labels should be removed or the whole group hidden for CPTs. | Major | Remove/condition the `hidden-written-by` pattern for `nadlan_project`, `nadlan_professional`, and `nadlan_property`; if using CSS as a bridge, hide the entire meta group, not only `wp-block-post-author/date/terms` children. |
| 8 | Submenu chevron has an approximately `8x8` tap target. | REAL on live 1.42.2. | Header nav button: `<button ... aria-label="תפריט משנה השקעה" class="wp-block-navigation__submenu-icon wp-block-navigation-submenu__toggle" aria-expanded="false"><svg ... width="12" height="12"...></svg></button>` with rect `{x:768,y:33,w:8,h:8}`. | WordPress navigation block outputs `.wp-block-navigation__submenu-icon` and core/block CSS collapses it to icon size. Current main 1.42.3 adds a plugin overlay in `inc/premium-ui.php:579-583` with `min-width:24px;min-height:24px;padding:8px`. | Minor | Ship/verify the 1.42.3 overlay or fix in the theme navigation block CSS. For mobile, confirm the effective tap region is at least 44px. |
| 9 | Extra flaw found: archive pages have duplicate H1s from the site title and the archive hero. | REAL on live 1.42.2. | `/projects/` H1s: `<h1><a href="https://nad-lan.co.il/">נדלן חכם</a></h1>` plus `<h1>פרויקטים והתחדשות עירונית</h1>`. `/professionals/` H1s: `<h1><a href="https://nad-lan.co.il/">נדלן חכם</a></h1>` plus `<h1>מצאו בעל מקצוע מאומת לנדל״ן</h1>`. | Mixed: plugin archive render calls `get_header()` and then prints its own archive hero H1 in `plugins/nadlan-config/inc/directory.php`; the active header/theme renders the site title as an H1 on these plugin archives. | Major | On plugin-owned archive routes, demote/hide the header site-title H1 or render the archive under a header variant that uses a non-H1 logo/site title. Keep the directory hero as the single page H1. |

## Ownership Notes

1. PLUGIN. Live renderer bug in `inc/directory.php`; current main appears fixed but production is not on that code.
2. MIXED. Theme template creates the extra H1; plugin creates the profile H1. Claude/plugin can bridge, but the clean fix is template-aware.
3. PLUGIN. Premium overlay/color token issue.
4. TOOLING-ARTIFACT. No page-content fix needed beyond #5.
5. PLUGIN. Honeypot CSS creates real desktop overflow.
6. THEME/BLOCKS plus PLUGIN. The first viewport is page/template owned; `.nhp-*` is plugin injected.
7. THEME/BLOCKS primarily. Plugin overlay can hide it, but the source is the `hidden-written-by` pattern in `templates/single.html`.
8. THEME/BLOCKS with acceptable PLUGIN overlay. The source element is WordPress navigation.
9. MIXED. Header/theme emits site-title H1; plugin emits archive H1.

## Could Not Reproduce / Limits

- I did not find a separate live "blank single page" bug. The blank 1440 screenshot reproduced only together with the off-screen honeypot overflow and disappeared after hiding `.nlcard-hp,.nlrev-hp` at runtime.
- I did not edit plugin code, CSS, routes, packaging, or versions.
