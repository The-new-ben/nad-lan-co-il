# Floor plans

Plan SVGs referenced by `projects.mock.ts`. AI-generated placeholders are
rendered inline (with permanent "Illustrative — AI generated" watermark) by
`MagazineCard` and `UnitSelector`; this folder is the upgrade slot for real
contractor plans.
